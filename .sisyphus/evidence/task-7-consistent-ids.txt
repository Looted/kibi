Scenario: Generate consistent IDs
Test: generates consistent content-based IDs
Status: PASSED

Evidence:
- Same YAML file parsed twice produces identical IDs
- ID format: 16-character hexadecimal string (first 16 chars of SHA256)
- Hash input: filePath + ":" + title
- Example: "a1b2c3d4e5f6g7h8"

This ensures:
1. Reproducible IDs across multiple parses
2. IDs change if file path or title changes
3. Consistent with markdown extractor pattern
