{
  "name": "kibi-codex",
  "version": "2.0.2",
  "description": "Kibi Codex plugin - thin adapter for Codex MCP integration",
  "type": "module",
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    ".codex-plugin",
    ".mcp.json",
    "hooks",
    "skills",
    "bin",
    "dist",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/Looted/kibi.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && bun run build:hook-bundle && bun run scripts/build-mcp-config.ts && bun run scripts/copy-plugin-assets.ts && bun run ../../scripts/sync-agent-skills.ts --write --target codex",
    "build:hook-bundle": "bun run scripts/build-hook-bundle.ts",
    "check:hook-bundle": "bun run scripts/build-hook-bundle.ts --check",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist",
    "prepack": "npm run build"
  },
  "peerDependencies": {
    "kibi-cli": ">=1.0.0",
    "kibi-mcp": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "kibi-cli": {
      "optional": true
    },
    "kibi-mcp": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5.7.0"
  }
}
