// implements REQ-codex-kibi-plugin-v1
export const packageName = "kibi-codex";
export const adapterKind = "codex-plugin";

const codexPlugin = {
  name: packageName,
  adapterKind,
};

export default codexPlugin;
