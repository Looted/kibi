# Bootstrap resource

`kb_autopilot_generate` is read-only. Review candidate evidence, entity types,
source locations, relationships, and diagnostics before approving. Keep writes
small and sequential; inspect each result before applying the next. A preview
does not authorize source edits, and a validation result does not prove fresh
end-to-end evidence.

