# Operation access

Use the same 21 operations through approved MCP or the trusted project-local
CLI. Parse `kibiProtocol`, `resultVersion`, `status`, `effects`, diagnostics,
and typed `nextActions`; do not retry a mutation after authoritative commit.

