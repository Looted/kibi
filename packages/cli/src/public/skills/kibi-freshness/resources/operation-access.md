# Operation access

Use visible approved MCP or the trusted project-local CLI. `kb_status`/`status`
is read-only and returns the shared versioned result envelope with exact branch,
store, freshness, effects, diagnostics, and typed next actions.

