// implements REQ-cursor-kibi-plugin-v1
export const packageName = "kibi-cursor";
export const adapterKind = "cursor-plugin";

const cursorPlugin = {
  name: packageName,
  adapterKind,
};

export default cursorPlugin;
