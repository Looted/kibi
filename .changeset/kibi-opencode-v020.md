---
"kibi-opencode": minor
---

Add kibi-opencode package for OpenCode integration. New plugin provides prompt guidance injection with sentinel-based dedupe, debounced single-flight sync scheduler, file filtering using Kibi sync semantics, plugin config with global/project precedence, and non-blocking UX with logging.
