---
"kibi-core": minor
"kibi-mcp": minor
"kibi-cli": minor
"kibi-opencode": patch
---

Add the curated discovery bundle across MCP and CLI, including search, status, gap analysis, coverage reporting, bounded graph traversal, richer `kb_check` summaries, and improved diagnostic usage logging. Update agent-facing guidance to prefer discovery-first workflows with `kb_search` and exact follow-up with `kb_query`.
