---
"kibi-cli": patch
"kibi-mcp": patch
"kibi-opencode": patch
---

Refresh README, CLI/MCP references, troubleshooting guidance, and agent-facing self-documentation to match the discovery bundle and current hook behavior.

This updates user-facing examples to prefer discovery-first workflows with `kibi search`/`kb_search`, documents the current curated public MCP tool surface, and removes stale `kibi init --hooks` guidance in favor of the default `kibi init` hook installation flow.
